const testName = "User Can Sign In";
const timeOut = 30;
const shouldRetry = true;

let responseBody: unknown = '{ status: "Ok" }';

if (typeof responseBody === "boolean") {
  console.log(responseBody);
} else {
  console.log("It's not a boolean!");
}
const Testername:string ="Mohamed GH";
let Shloudgget : unknown = true;
let anotherVariable: any = "testername";
let Resbody =  "status: Ok.";
if (anotherVariable==="tester name") {
  console.log(anotherVariable + " " + "Esmo Abdo" + " " + "and" + " " + Resbody);
}